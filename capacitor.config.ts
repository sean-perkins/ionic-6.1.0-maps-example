import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-6.1.0-sheet-modal-maps-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
